# Supabase Configuration
# Get these from your Supabase project dashboard (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# REQUIRED: Service Role Key for admin operations
# This is needed for all admin API endpoints (Downloads, News, Site Settings, etc.)
# Without this, you'll see 500 errors when using the admin panel
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Admin Authentication
# Set a strong password to protect the admin area
ADMIN_PASSWORD=your_secure_admin_password

# Google Gemini AI (Optional)
# Required only if you want to use the AI Assistant feature
GEMINI_API_KEY=your_gemini_api_key
# Optional: Specify a different model (default is gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# TinyMCE (Optional)
# Only needed if you want to use cloud features of TinyMCE
# NEXT_PUBLIC_TINYMCE_API_KEY=your_tinymce_api_key
